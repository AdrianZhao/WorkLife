@model IEnumerable<WorkLife.Models.Job>
@using Microsoft.AspNetCore.Identity

@{
    ViewData["Title"] = "Index";
    var userManager = Context.RequestServices.GetService<UserManager<WorkLife.Areas.Identity.Data.WorkLifeUser>>();
    var currentUserId = userManager.GetUserName(User);
}

<h1>All Jobs</h1>

<p>
    @if (User.IsInRole("Employer"))
    {
        <a asp-action="Create">Create New Job</a>
    }
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Duration)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Competition)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Location)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StartDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SubmissionDeadline)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IndustryAreas)
            </th>           
            <th>
                Total applications
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
    @foreach (var item in Model) 
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Duration)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Competition)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Location)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StartDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SubmissionDeadline)
            </td>
            <td>
                @foreach (var industryArea in item.IndustryAreas)
                {
                    @industryArea.IndustryArea.Title
                    @if (industryArea != item.IndustryAreas.Last())
                    {
                        <text>, </text>
                    }
                }
            </td>
            <td>
                @if (User.IsInRole("Employer"))
                {
                    @if (@item.Applications.Count() > 0)
                    {
                        <a asp-action="Applications" asp-route-id="@item.Id">@item.Applications.Count()</a>
                    }
                    else
                    {
                        <p>No submissions</p>
                    }
                }
            </td>
            <td>
                @if (User.Identity.IsAuthenticated)
                {                   
                    bool isCreator = item.EmployerEmail == currentUserId;
                    if (isCreator)
                    {
                        <div>
                            <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                            <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                        </div>
                    }
                    else
                    { 
                        if (User.IsInRole("Applicant"))
                        {
                            bool isBeforeSubmissionDeadline = DateTime.Now.Date <= item.SubmissionDeadline;
                            <div>
                                @if (isBeforeSubmissionDeadline)
                                {
                                    <a asp-action="Apply" asp-route-id="@item.Id">Apply</a>
                                }
                                else
                                {
                                    <span>Submission Deadline Passed</span>
                                }
                            </div>
                        }
                    }
                }
            </td>
        </tr>
    }
    </tbody>
</table>
